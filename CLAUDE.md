# Commit messages

Never add a `Co-Authored-By: Claude ...` (or any Anthropic/Claude co-author) trailer to commit
messages in this repository, regardless of default tooling behavior. This repo's GitHub
Contributors list must not include Claude.
